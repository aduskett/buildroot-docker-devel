version: "3.8"
services:
   br-jammy:
    build:
      context: ./
    environment:
      - TZ=America/Los_Angeles
      - DEBIAN_FRONTEND=noninteractive
    privileged: true
    stdin_open: true
    tty: true
    image: br-jammy:latest
    container_name:  br-jammy
